/*global define, describe, it, beforeEach, expect, spyOn, require */
define(function (require) {
  describe("Particle", function() {
    var Particle = require('app/particle');

    it("should pass a sanity check", function() {
      expect(true).toBe(true);
    });
  });
});
